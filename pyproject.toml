[project]
name = "audionomy"
version = "0.1.0"
description = "An autonomous audio library builder"
authors = [
    {name = "Sandeep Paidipati",email = "sandeep.paidipati@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = "^3.9"
dependencies = [
    "django (>=4.1,<5.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
